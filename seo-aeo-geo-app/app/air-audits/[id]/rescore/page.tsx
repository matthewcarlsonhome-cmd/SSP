export default function AirRescorePage() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8">
      <h1 className="text-3xl font-bold">AIR Re-score</h1>
      <p className="mt-2 text-muted-foreground">
        Re-scores create a child AIR audit, copy prior evidence forward, refresh changed inputs, and generate a delta report. The schema is in place; the full copy-forward workflow is planned for the next AIR milestone.
      </p>
    </main>
  );
}
